<Window x:Class="BackupCleaner.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instellingen - Lightroom Classic Backup Cleaner" 
        Height="450" Width="550"
        MinHeight="400" MinWidth="500"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⚙️ Instellingen" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Configureer automatische opruiming" 
                       FontSize="14" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Automatisch opruimen sectie -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🕐 Automatisch opruimen" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,8"/>
                        
                        <TextBlock Text="Wanneer ingeschakeld, ruimt de applicatie dagelijks automatisch oude Lightroom backups op. De applicatie moet actief zijn (eventueel in de system tray)." 
                                   TextWrapping="Wrap"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,16"/>
                        
                        <CheckBox x:Name="chkAutoCleanup" 
                                  Content="Automatisch dagelijks opruimen"
                                  Style="{StaticResource ModernCheckBoxStyle}"
                                  Checked="ChkAutoCleanup_Changed"
                                  Unchecked="ChkAutoCleanup_Changed"/>
                        
                        <!-- Tijdstip instelling -->
                        <StackPanel x:Name="timeSettings" 
                                    Orientation="Horizontal" 
                                    Margin="30,16,0,0"
                                    Visibility="Collapsed">
                            <TextBlock Text="Opruimen om:" 
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,12,0"/>
                            <Border Background="{StaticResource SecondaryBrush}" 
                                    CornerRadius="4" 
                                    BorderBrush="{StaticResource BorderBrush}" 
                                    BorderThickness="1"
                                    Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="−" 
                                            Click="BtnDecreaseHour_Click" 
                                            Width="28" Height="28"
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Foreground="{StaticResource TextPrimaryBrush}"
                                            FontWeight="Bold"
                                            FontSize="16"
                                            Cursor="Hand"/>
                                    <TextBlock x:Name="txtCleanupHour" 
                                               Text="02:00" 
                                               Width="50" 
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontWeight="SemiBold"
                                               FontSize="15"/>
                                    <Button Content="+" 
                                            Click="BtnIncreaseHour_Click" 
                                            Width="28" Height="28"
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Foreground="{StaticResource TextPrimaryBrush}"
                                            FontWeight="Bold"
                                            FontSize="16"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </Border>
                            <TextBlock Text="uur" 
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="8,0,0,0"/>
                        </StackPanel>
                        
                        <TextBlock x:Name="txtAutoCleanupInfo" 
                                   Text="" 
                                   FontSize="12" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="30,12,0,0"
                                   Visibility="Collapsed"/>
                        
                        <!-- Info box -->
                        <Border Background="#1E3A5F" 
                                CornerRadius="6" 
                                Padding="12" 
                                Margin="0,16,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="💡" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="1" 
                                           Text="Een Windows Scheduled Task wordt aangemaakt zodat de opruiming ook werkt als de app niet actief is." 
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontSize="12"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Over sectie -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="ℹ️ Over deze applicatie" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,8"/>
                        
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   LineHeight="22">
                            <Run Text="Lightroom Classic Backup Cleaner helpt je oude catalogus backups van Adobe Lightroom Classic automatisch op te ruimen."/>
                            <LineBreak/><LineBreak/>
                            <Run Text="De applicatie detecteert automatisch je Lightroom backup locatie en toont alle beschikbare backups. Je kunt instellen hoeveel backups je wilt bewaren en hoe oud een backup minimaal moet zijn voordat deze verwijderd mag worden."/>
                        </TextBlock>
                        
                        <Border Background="{StaticResource SecondaryBrush}" 
                                CornerRadius="6" 
                                Padding="12" 
                                Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Gemaakt door " 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="lnkPhotofacts"
                                           Text="Photofacts Academy" 
                                           Foreground="{StaticResource AccentBrush}"
                                           FontWeight="SemiBold"
                                           Cursor="Hand"
                                           TextDecorations="Underline"
                                           MouseLeftButtonDown="PhotofactsLink_Click"
                                           VerticalAlignment="Center"
                                           ToolTip="https://photofactsacademy.nl"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Margin="0,16,0,0">
            <Button Content="Sluiten" 
                    Click="BtnClose_Click"
                    Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Right"
                    MinWidth="100"/>
        </Border>
    </Grid>
</Window>
