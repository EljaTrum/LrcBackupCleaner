<Window x:Class="BackupCleaner.CleanupPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Te Verwijderen Backups - Preview" 
        Height="550" Width="750"
        MinHeight="400" MinWidth="600"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="⚠️ Te Verwijderen Backups" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="{StaticResource WarningBrush}"/>
                <TextBlock x:Name="txtSummary" 
                           FontSize="14" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,8,0,0"/>
                <TextBlock Text="Controleer onderstaande backups voordat je ze definitief verwijdert." 
                           FontSize="13" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,4,0,0"
                           FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- Backup List -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- List Header -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Backup Map" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Grid.Column="1" Text="Backup Datum" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Grid.Column="2" Text="Grootte" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" TextAlignment="Right"/>
                </Grid>
                
                <!-- List Content -->
                <ListView x:Name="lstFiles" 
                          Grid.Row="1" 
                          BorderThickness="0"
                          Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0,10"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{StaticResource BorderBrush}" 
                                                BorderThickness="0,0,0,1" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#2D2D2D"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#1E3A5F"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" 
                                           Text="{Binding FileName}" 
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding FilePath}"/>
                                
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding BackupDateFormatted}" 
                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                
                                <TextBlock Grid.Column="2" 
                                           Text="{Binding SizeFormatted}" 
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           TextAlignment="Right"
                                           FontWeight="SemiBold"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       x:Name="txtWarning"
                       Text="🔴 Let op: Deze actie kan niet ongedaan worden gemaakt!"
                       Foreground="{StaticResource DangerBrush}"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"/>
            
            <Button Grid.Column="1" 
                    x:Name="btnCancel"
                    Content="Annuleren" 
                    Click="BtnCancel_Click"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="0,0,12,0"/>
            
            <Button Grid.Column="2" 
                    x:Name="btnConfirm"
                    Content="🗑️ Definitief Verwijderen" 
                    Click="BtnConfirm_Click"
                    Style="{StaticResource AccentButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
